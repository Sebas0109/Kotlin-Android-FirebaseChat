<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:editTextColor">@color/colorPrimaryText</item>
        <item name="android:textColorHint">@color/colorSecondaryText</item>

        <item name="android:windowNoTitle">true</item>
    </style>

    <!--PARA EVITAR EL PARPADEO DE LA PREVIEW DEL ACTIVITY PONEMOS ESTE ESTILO Y LO AGREGAMOS DEONDE SEA CORRESPONDIENTE POR ACTIVITY ESTE SE PONE EN EL MAINFEST DONDE SEA LA ACTIVITY CORRESPONDIENTE-->
    <style name="NoPreview" parent="AppTheme">
        <item name="android:windowDisablePreview">true</item>
    </style>

    <style name="custom_rating_bar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/rating_bar_custom</item>
        <item name="android:minHeight">48dip</item>
        <item name="android:maxHeight">48dip</item>
    </style>

</resources>
